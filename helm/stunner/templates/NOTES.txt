{{ .Chart.Name }} successfully installed
{{- if eq .Values.stunner.withoutGatewayOperator false}}
You installation is depended on the stunner-gateway-operator.
If have not already installed it you can do it with the following command:
<helm install stunner-gateway-operator.....>
To checkout the installed resources:
$ kubectl get pod -n {{ .Values.stunner.namespace }}
{{- else}}
You installed stunner without the gateway operator in a standalone mode.
!IMPORTANT!
Customize the default values below stunner.config
to your own specification!
{{- end}}
